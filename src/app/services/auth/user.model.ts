export interface User {
    imageURL: string;
    fullname: string;
    createdOn?: any,
    email: string
    is_completed?: boolean,
    living_since?: number,
    locality?: string,
    uid: string,
};